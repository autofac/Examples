name: Continuous Integration

on:
  push:
    branches:
      - main
      - develop
      - "feature/**"
  pull_request:
    branches:
      - develop

jobs:
  build:
    uses: ./.github/workflows/build.yml

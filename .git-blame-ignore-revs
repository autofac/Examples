# Ignore revisions in git blame - set your git config to use the file by convention:
# git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Optional additional git config:
# Mark any lines that have had a commit skipped using --ignore-rev with a `?`
# git config --global blame.markIgnoredLines true
# Mark any lines that were added in a skipped commit and can not be attributed with a `*`
# git config --global blame.markUnblamableLines true

# Convert to file-scoped namespaces.
ccd9b352d4066839cbe2dff010df6ab464ea871e

# Normalize whitespace and JSON key order via pre-commit.
39b63732ecc81559303a7f1ab9d1283c37b279c9

# Apply dotnet format across the solution.
2fea688003a3947a656d91e0532cd2dff2a737c6

# Convert to file-scoped namespaces and drop redundant usings.
f089e7856c50b437f6583ba069a3ed553d1f7d61
